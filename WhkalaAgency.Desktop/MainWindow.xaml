<Window x:Class="WhkalaAgency.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="نظام إدارة الوكالة الذكي"
        Height="900"
        Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft"
        Background="#F4F7FA"
        FontFamily="Segoe UI Semibold">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue"
                Color="#1976D2"/>
        <SolidColorBrush x:Key="SidebarText"
                Color="#4A5568"/>

        <Style x:Key="NavButtonStyle"
                TargetType="RadioButton">
            <Setter Property="Height"
                    Value="50"/>
            <Setter Property="Margin"
                    Value="15,2"/>
            <Setter Property="GroupName"
                    Value="MainNavigation"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Border x:Name="border"
                                    CornerRadius="10"
                                    Background="Transparent">
                                <StackPanel Orientation="Horizontal"
                                        Margin="15,0">
                                    <TextBlock x:Name="icon"
                                            Text="{TemplateBinding Tag}"
                                               FontFamily="Segoe UI Symbol"
                                            FontSize="18"
                                               VerticalAlignment="Center"
                                            Margin="0,0,12,0"
                                               Foreground="{StaticResource SidebarText}"/>
                                    <ContentPresenter VerticalAlignment="Center"
                                            x:Name="content"/>
                                </StackPanel>
                            </Border>
                            <Border x:Name="indicator"
                                    HorizontalAlignment="Right"
                                    Width="4"
                                    Height="20"
                                    Background="{StaticResource PrimaryBlue}"
                                    CornerRadius="2"
                                    Visibility="Collapsed"
                                    Margin="0,0,-15,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#EDF2F7"/>
                                <Setter TargetName="icon"
                                        Property="Foreground"
                                        Value="{StaticResource PrimaryBlue}"/>
                            </Trigger>
                            <Trigger Property="IsChecked"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#EBF8FF"/>
                                <Setter TargetName="icon"
                                        Property="Foreground"
                                        Value="{StaticResource PrimaryBlue}"/>
                                <Setter Property="Foreground"
                                        Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="indicator"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize"
                    Value="15"/>
            <Setter Property="Foreground"
                    Value="{StaticResource SidebarText}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="White"
                BorderBrush="#E2E8F0"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top"
                        Margin="30,40,30,40">
                    <Border Width="50"
                            Height="50"
                            Background="#1976D2"
                            CornerRadius="12"
                            HorizontalAlignment="Right"
                            Margin="0,0,0,15">
                        <TextBlock Text="W"
                                Foreground="White"
                                FontSize="24"
                                FontWeight="Bold"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="الوكالة الذكية"
                            FontSize="22"
                            FontWeight="Black"
                            Foreground="#1A202C"/>
                    <TextBlock Text="إدارة المخزون والمالية"
                            FontSize="12"
                            Foreground="#718096"
                            Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom"
                        Margin="15,0,15,30">
                    <Separator Background="#F7FAFC"
                            Margin="15,10"/>
                    <Button Content="⚙️ الإعدادات"
                            Click="Settings_Click"
                            HorizontalContentAlignment="Right"
                            Padding="15,10"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="#4A5568"
                            FontSize="14"
                            Cursor="Hand"/>
                    <Button Content="🚪 تسجيل الخروج"
                            Click="Exit_Click"
                            HorizontalContentAlignment="Right"
                            Padding="15,10"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="#E53E3E"
                            FontWeight="Bold"
                            FontSize="14"
                            Cursor="Hand"/>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <StackPanel>
                       

                        <RadioButton Content="لوحة التحكم"
                                Tag=""
                                IsChecked="True"
                                Style="{StaticResource NavButtonStyle}"
                                Click="Dashboard_Click"/>

                      
                        <RadioButton Content="الأصناف والمنتجات"
                                Tag=""
                                Style="{StaticResource NavButtonStyle}"
                                Click="Products_Click"/>
                        <RadioButton Content="حسابات المزارعين"
                                Tag=""
                                Style="{StaticResource NavButtonStyle}"
                                Click="Farmers_Click"/>
                        <RadioButton Content="سجل التجار"
                                Tag=""
                                Style="{StaticResource NavButtonStyle}"
                                Click="Traders_Click"/>

                
                        <RadioButton Content="توريد محاصيل"
                                Tag=""
                                Style="{StaticResource NavButtonStyle}"
                                Click="Supply_Click"/>
                        <RadioButton Content="فواتير المبيعات"
                                Tag=""
                                Style="{StaticResource NavButtonStyle}"
                                Click="Sale_Click"/>

                       
                        <RadioButton x:Name="UsersButton"
                                Content="إدارة المستخدمين"
                                Tag=""
                                Style="{StaticResource NavButtonStyle}"
                                Click="Users_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="White"
                    Padding="40,0"
                    BorderBrush="#E2E8F0"
                    BorderThickness="0,0,0,1">
                <DockPanel VerticalAlignment="Center">
                    <StackPanel DockPanel.Dock="Right">
                        <TextBlock x:Name="CurrentUserTextBlock"
                                Text="مرحباً، أحمد ممدوح"
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="#2D3748"/>
                        <TextBlock Text="مدير النظام"
                                FontSize="12"
                                Foreground="#718096"
                                HorizontalAlignment="Right"/>
                    </StackPanel>

                    <Border DockPanel.Dock="Left"
                            Background="#F7FAFC"
                            CornerRadius="8"
                            Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📅"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"/>
                            <TextBlock x:Name="DateTimeTextBlock"
                                    Text="4 فبراير، 2026"
                                    FontSize="13"
                                    Foreground="#4A5568"
                                    VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DockPanel>
            </Border>

            <Border Grid.Row="1"
                    Margin="30"
                    Background="White"
                    CornerRadius="16">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30"
                            Opacity="0.05"
                            ShadowDepth="4"
                            Color="#A0AEC0"/>
                </Border.Effect>
                <ContentControl x:Name="MainContent"/>
            </Border>
        </Grid>
    </Grid>
</Window>