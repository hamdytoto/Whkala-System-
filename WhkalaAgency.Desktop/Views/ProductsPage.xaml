<UserControl x:Class="WhkalaAgency.Desktop.Views.ProductsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">

    <Grid Background="#F4F7FA" TextOptions.TextRenderingMode="ClearType" RenderOptions.ClearTypeHint="Enabled">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="30,25,30,20">
            <TextBlock Text="دليل الأصناف" FontSize="32" FontWeight="Black" Foreground="#1A202C" />
            <TextBlock Text="إدارة المنتجات، تصنيفها، ومتابعة المخزون الحالي" FontSize="14" Foreground="#718096" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" Margin="30,0,30,20" Padding="15" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <DockPanel>
                <Button x:Name="BtnAddProduct" 
                        DockPanel.Dock="Left" 
                        Click="BtnAddProduct_Click"
                        Style="{StaticResource PrimaryButton}" 
                        Cursor="Hand"
                        FontSize="16" 
                        FontWeight="Bold" 
                        Padding="20,5" 
                        Height="45" 
                        HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="➕" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="إضافة صنف جديد" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#F7FAFC" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Width="400" Height="45">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#A0AEC0" FontSize="16"/>
                            <TextBox x:Name="TxtSearch" Grid.Column="1" Background="Transparent" BorderThickness="0"
                                     VerticalContentAlignment="Center" FontSize="16" Foreground="#2D3748"
                                     Padding="5,0" TextChanged="TxtSearch_TextChanged"
                                     Tag="ابحث عن اسم الصنف أو التصنيف..."/>
                        </Grid>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border x:Name="ProductForm" Grid.Row="2" Visibility="Collapsed"
                Background="White" BorderBrush="#3182CE" BorderThickness="0,4,0,0"
                CornerRadius="12" Padding="25" Margin="30,0,30,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="بيانات الصنف" FontSize="20" FontWeight="Black" Margin="0,0,0,20" Foreground="#2D3748"/>
                <UniformGrid Columns="4">
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="اسم الصنف" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4A5568"/>
                        <Border Background="#F7FAFC" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1">
                            <TextBox x:Name="TxtProductName" Height="42" Background="Transparent" BorderThickness="0" FontSize="16" Padding="10,0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="التصنيف" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4A5568"/>
                        <Border Background="#F7FAFC" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1">
                            <ComboBox x:Name="ComboCategory" Height="42" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="وحدة القياس" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4A5568"/>
                        <Border Background="#F7FAFC" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1">
                            <ComboBox x:Name="ComboUnit" Height="42" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="المخزون الحالي" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4A5568"/>
                        <Border Background="#F7FAFC" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1">
                            <TextBox x:Name="TxtStock" Height="42" Background="Transparent" BorderThickness="0" FontSize="16" Padding="10,0" VerticalContentAlignment="Center" Text="0"/>
                        </Border>
                    </StackPanel>
                </UniformGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,25,0,0">
                    <Button Content="حفظ البيانات" Width="140" Height="45" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click" Margin="0,0,12,0"/>
                    <Button Content="إلغاء" Width="90" Height="45" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="White" Margin="30,0,30,30" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" ClipToBounds="True">
            <DataGrid x:Name="ProductsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="65"
                      FontSize="16" Background="Transparent" BorderThickness="0">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F9FB"/>
                        <Setter Property="Foreground" Value="#4A5568"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="20,15"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="الصنف" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="20,0">
                                    <Border Width="32" Height="32" CornerRadius="8" Background="#FFF5F5" Margin="0,0,12,0">
                                        <TextBlock Text="📦" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14"/>
                                    </Border>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#2D3748"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="التصنيف" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="الوحدة" Binding="{Binding Unit}" Width="120"/>

                    <DataGridTemplateColumn Header="المخزون" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#EBF8FF" CornerRadius="6" Padding="10,4" HorizontalAlignment="Left" Margin="20,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding CurrentStock}" FontWeight="Bold" Foreground="#2B6CB0"/>
                                        <TextBlock Text="{Binding Unit}" Margin="5,0,0,0" FontSize="12" Foreground="#4A5568"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="إجراءات" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="📝" Background="Transparent" BorderThickness="0" Click="BtnEdit_Click" Cursor="Hand" Margin="0,0,10,0"/>
                                    <Button Content="🗑️" Background="Transparent" BorderThickness="0" Click="BtnDelete_Click" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>