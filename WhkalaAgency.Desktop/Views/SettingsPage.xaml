<UserControl x:Class="WhkalaAgency.Desktop.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             Background="#F2F6FB">

    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" Opacity="0.08" ShadowDepth="2" Color="#1E2A3A"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="BorderBrush" Value="#E6EBF2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFCFF"/>
            <Setter Property="BorderBrush" Value="#D8E2EF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="FieldPasswordStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFCFF"/>
            <Setter Property="BorderBrush" Value="#D8E2EF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="FieldComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFCFF"/>
            <Setter Property="BorderBrush" Value="#D8E2EF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="15" Padding="22,16" Margin="0,0,0,14">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#1E3A8A" Offset="0"/>
                    <GradientStop Color="#1D4ED8" Offset="0.58"/>
                    <GradientStop Color="#2563EB" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <TextBlock Text="إعدادات النظام" FontSize="30" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="إدارة المعاملات العامة، المستخدمين، والنسخ الاحتياطي" FontSize="14" Foreground="#D9E6FF" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1">
            <TabItem Header="الإعدادات العامة">
                <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                    <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                        <TextBlock Text="نسبة عمولة الوكالة (مثال: 0.05 تعني 5%)" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#42526A"/>
                        <TextBox x:Name="TxtCommissionRate" Style="{StaticResource FieldTextBoxStyle}" Margin="0,0,0,18"/>

                        <Button x:Name="BtnSaveGeneralSettings"
                                Content="حفظ الإعدادات العامة"
                                Click="BtnSaveGeneralSettings_Click"
                                Background="#2563EB"
                                Foreground="White"
                                Height="44"
                                FontWeight="Bold"
                                BorderThickness="0"/>
                    </StackPanel>
                </Border>
            </TabItem>

            <TabItem Header="إدارة المستخدمين">
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="360"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="إضافة مستخدم جديد" FontSize="18" FontWeight="Bold" Margin="0,0,0,14" Foreground="#1F2A3A"/>

                            <TextBlock Text="اسم المستخدم" Foreground="#4B5A70" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtUsername" Style="{StaticResource FieldTextBoxStyle}" Margin="0,0,0,10"/>

                            <TextBlock Text="الاسم الكامل" Foreground="#4B5A70" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtFullName" Style="{StaticResource FieldTextBoxStyle}" Margin="0,0,0,10"/>

                            <TextBlock Text="كلمة المرور" Foreground="#4B5A70" Margin="0,0,0,6"/>
                            <PasswordBox x:Name="TxtPassword" Style="{StaticResource FieldPasswordStyle}" Margin="0,0,0,10"/>

                            <TextBlock Text="الصلاحية" Foreground="#4B5A70" Margin="0,0,0,6"/>
                            <ComboBox x:Name="CboRole" Style="{StaticResource FieldComboStyle}" Margin="0,0,0,18">
                                <ComboBoxItem Content="مدير نظام (Admin)" IsSelected="True" Tag="Admin"/>
                                <ComboBoxItem Content="موظف (Operator)" Tag="Operator"/>
                            </ComboBox>

                            <Button x:Name="BtnAddUser"
                                    Content="إضافة المستخدم"
                                    Click="BtnAddUser_Click"
                                    Background="#16A34A"
                                    Foreground="White"
                                    Height="42"
                                    FontWeight="Bold"
                                    BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                        <DataGrid x:Name="UsersGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="None"
                                  HeadersVisibility="Column"
                                  RowHeight="40"
                                  BorderThickness="0"
                                  Background="White">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F7FAFE"/>
                                    <Setter Property="Foreground" Value="#42526A"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="14,12"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#E3EAF3"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#EEF2F7"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F8FBFF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الاسم الكامل" Binding="{Binding FullName}" Width="*"/>
                                <DataGridTextColumn Header="اسم المستخدم" Binding="{Binding Username}" Width="*"/>
                                <DataGridTextColumn Header="الصلاحية" Binding="{Binding Role}" Width="120"/>
                                <DataGridTemplateColumn Width="90" Header="">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="حذف" Click="BtnDeleteUser_Click" Background="Transparent" BorderThickness="0" Foreground="#C53030" FontWeight="Bold" Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="قاعدة البيانات">
                <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                    <StackPanel MaxWidth="560" HorizontalAlignment="Left">
                        <TextBlock Text="النسخ الاحتياطي" FontSize="20" FontWeight="Bold" Margin="0,0,0,8" Foreground="#1F2A3A"/>
                        <TextBlock Text="يوصى بأخذ نسخة احتياطية يومياً لضمان سلامة البيانات."
                                   Foreground="#71839A"
                                   Margin="0,0,0,18"/>

                        <Button x:Name="BtnBackup"
                                Content="إنشاء نسخة احتياطية الآن (Backup)"
                                Click="BtnBackup_Click"
                                Background="#2563EB"
                                Foreground="White"
                                Height="44"
                                FontWeight="Bold"
                                BorderThickness="0"/>

                        <TextBlock Text="سجل العمليات الأخيرة" FontWeight="SemiBold" Margin="0,22,0,10" Foreground="#4B5A70"/>

                        <DataGrid x:Name="BackupsGrid"
                                  AutoGenerateColumns="False"
                                  Height="220"
                                  GridLinesVisibility="None"
                                  HeadersVisibility="Column"
                                  RowHeight="38"
                                  BorderThickness="1"
                                  BorderBrush="#E6EBF2">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F7FAFE"/>
                                    <Setter Property="Foreground" Value="#42526A"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#E3EAF3"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAt}" Width="170"/>
                                <DataGridTextColumn Header="المسار" Binding="{Binding BackupPath}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
