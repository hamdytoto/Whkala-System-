<UserControl x:Class="WhkalaAgency.Desktop.Views.SupplyPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="BorderBrush" Value="#E6EBF2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" Opacity="0.08" ShadowDepth="2" Color="#1E2A3A"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFCFF"/>
            <Setter Property="BorderBrush" Value="#D8E2EF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="FieldComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFCFF"/>
            <Setter Property="BorderBrush" Value="#D8E2EF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4B5A70"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F2F6FB">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="26,20,26,14" Padding="24,18" CornerRadius="16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#1E3A8A" Offset="0"/>
                    <GradientStop Color="#1D4ED8" Offset="0.55"/>
                    <GradientStop Color="#2563EB" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="إدارة استلام التوريدات" FontSize="31" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="نظام الإذن المتعدد - تسجيل مالي وتخزيني"
                               FontSize="14"
                               Margin="0,6,0,0"
                               Foreground="#D6E4FF"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="تاريخ التوريد" Foreground="#D6E4FF" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker x:Name="DtSupplyDate" Width="170" Height="38"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="26,0,26,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="بيانات الصنف المورد" FontWeight="Bold" Foreground="#1F2A3A" Margin="0,0,0,12" FontSize="16"/>

                    <TextBlock Text="المزارع (صاحب البضاعة)" Style="{StaticResource SectionLabelStyle}"/>
                    <ComboBox x:Name="CboFarmers" DisplayMemberPath="Name" SelectedValuePath="Id" Style="{StaticResource FieldComboStyle}" Margin="0,0,0,10"/>

                    <TextBlock Text="الصنف" Style="{StaticResource SectionLabelStyle}"/>
                    <ComboBox x:Name="CboProducts" DisplayMemberPath="Name" SelectedValuePath="Id" Style="{StaticResource FieldComboStyle}" Margin="0,0,0,10"/>

                    <UniformGrid Columns="2" Margin="0,0,0,8">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="العدد (عبوة/شكارة)" Style="{StaticResource SectionLabelStyle}"/>
                            <TextBox x:Name="TxtQty" Style="{StaticResource FieldTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="السعر المتوقع" Style="{StaticResource SectionLabelStyle}"/>
                            <TextBox x:Name="TxtExpectedPrice" Style="{StaticResource FieldTextBoxStyle}"/>
                        </StackPanel>
                    </UniformGrid>

                    <UniformGrid Columns="3" Margin="0,0,0,6">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="الوزن القائم" Style="{StaticResource SectionLabelStyle}"/>
                            <TextBox x:Name="TxtGrossWeight" TextChanged="CalculateWeights" Style="{StaticResource FieldTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="الفارغ" Style="{StaticResource SectionLabelStyle}"/>
                            <TextBox x:Name="TxtTareWeight" TextChanged="CalculateWeights" Style="{StaticResource FieldTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="الصافي" FontSize="12" FontWeight="Bold" Foreground="#1D4E89" Margin="0,0,0,6"/>
                            <TextBox x:Name="TxtNetWeight" IsReadOnly="True" FontWeight="Bold" Style="{StaticResource FieldTextBoxStyle}" Background="#EEF6FF" BorderBrush="#B9D6F7"/>
                        </StackPanel>
                    </UniformGrid>

                    <Button Content="إضافة للصنف الحالي"
                            Click="BtnAddToBasket_Click"
                            Height="42"
                            Margin="0,8,0,0"
                            FontWeight="Bold"
                            FontSize="14"
                            Background="#2563EB"
                            Foreground="White"
                            BorderThickness="0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <DockPanel LastChildFill="True">
                    <TextBlock DockPanel.Dock="Top" Text="الأصناف المدرجة في هذا الإذن" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#42526A"/>

                    <Button DockPanel.Dock="Bottom"
                            Content="تأكيد وحفظ إذن التوريد"
                            Click="BtnSaveAndPrint_Click"
                            Background="#16A34A"
                            Foreground="White"
                            Height="44"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Margin="0,10,0,0"/>

                    <Border BorderBrush="#E5ECF6" BorderThickness="1" CornerRadius="10">
                        <DataGrid x:Name="BasketGrid"
                                  AutoGenerateColumns="False"
                                  Background="White"
                                  BorderThickness="0"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="None"
                                  HeadersVisibility="Column"
                                  RowHeight="38">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F7FAFE"/>
                                    <Setter Property="Foreground" Value="#42526A"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#E3EAF3"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الصنف" Binding="{Binding ProductName}" Width="*"/>
                                <DataGridTextColumn Header="العدد" Binding="{Binding Quantity}" Width="60"/>
                                <DataGridTextColumn Header="الصافي" Binding="{Binding NetWeight}" Width="75"/>
                                <DataGridTextColumn Header="السعر" Binding="{Binding ExpectedPrice}" Width="80"/>
                                <DataGridTextColumn Header="صافي المزارع" Binding="{Binding NetToFarmer, StringFormat={}{0:N2}}" Width="115">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="#1F8A5B"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Width="70" Header="">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="حذف"
                                                    Foreground="#C53030"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Click="RemoveFromBasket_Click"
                                                    FontSize="12"
                                                    Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Margin="26,0,26,22" Style="{StaticResource CardBorderStyle}" Padding="0" ClipToBounds="True">
            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Top" Padding="16,12" Background="#F7FAFE" BorderBrush="#E3EAF3" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="بحث برقم الإذن" VerticalAlignment="Center" Foreground="#4B5A70" Margin="0,0,8,0"/>
                        <TextBox x:Name="TxtSearch" Width="190" Height="34" TextChanged="FilterHistory_Changed"/>

                        <TextBlock Text="المزارع" VerticalAlignment="Center" Foreground="#4B5A70" Margin="16,0,8,0"/>
                        <ComboBox x:Name="CboFilterFarmer"
                                  Width="170"
                                  Height="34"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Id"
                                  SelectionChanged="FilterHistory_Changed"/>
                    </StackPanel>
                </Border>

                <DataGrid x:Name="SuppliesGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowHeight="40"
                          BorderThickness="0"
                          Background="White"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F7FAFE"/>
                            <Setter Property="Foreground" Value="#42526A"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="14,12"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E3EAF3"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#EEF2F7"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F8FBFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الإذن" Binding="{Binding BatchNumber}" Width="150"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding SupplyDate}" Width="120"/>
                        <DataGridTextColumn Header="المزارع" Binding="{Binding FarmerName}" Width="*"/>
                        <DataGridTextColumn Header="الصنف" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="العدد" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="الوزن الصافي" Binding="{Binding NetWeight}" Width="110"/>
                        <DataGridTextColumn Header="صافي القيمة" Binding="{Binding Total, StringFormat={}{0:N2}}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
